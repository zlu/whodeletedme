require 'spec_helper'

describe Dumpee do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Dumpee.create!(@valid_attributes)
  end
end
