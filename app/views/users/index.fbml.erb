All <%= @user_ids.size %> users currently in our database.<br /><br />

<% unless @user_ids.nil? || @user_ids.empty? %>
      <% for fb_id in @user_ids %>
          <%= fb_profile_pic fb_id %>
          <%= fb_name fb_id %> (<%= fb_id %>)
          <br/>
      <% end %>
<% end %>